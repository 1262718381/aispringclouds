spring:
  cloud:
    config:
      label: master
      discovery:
        enabled: true
        service-id: config-servers
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/